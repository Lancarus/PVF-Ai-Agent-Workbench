不能直接照抄或复制 B 站教程和本地 nut攻略写入目标 PVF；这些只能作为 source-clue 线索，不能当事实。先确认目标 Script.pvf、输出授权和实机验证条件，再只读解析职业 registry、skill .lst、目标 .skl、load_state、pushState、pushPassiveObj，并把教程里的技能 ID、APID、PO ID、state/substate 都回到正确 .lst 和目标脚本核对。API 名优先查 TypeSquirrel；没有工具时只标候选。dofile/sq_RunScript 和调试 UI 只属于本地调试线索，不能当发布 PVF 方案。写出前必须 dry-run、备份、显式输出、raw no-simplified 替换、读回和 manifest，BUFF/PO/图标/伤害/同步仍需实机验证。
