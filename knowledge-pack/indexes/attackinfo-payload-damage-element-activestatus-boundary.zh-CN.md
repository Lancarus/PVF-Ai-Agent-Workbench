# AttackInfo Payload / Damage / Element / ActiveStatus Boundary

状态：需验证


## 方法边界

- `.atk` 字段必须放回 owner 链路和 `.ani` hitbox 才能讨论命中；当前只封闭静态 payload 边界。


| 小桶 | 目标核验 | 结论 |
| --- | --- | --- |
| 文件范围 | 需在当前目标 PVF 中只读确认 | 本主题封闭 `passiveobject/` `.atk` payload 小桶，不代表全域 owner/hitbox 已穷尽。 |
| 基础 payload | 需在当前目标 PVF 中只读确认 | `.atk` 可携带攻击类型和敌我入口；不能静态证明命中规则。 |
| 伤害入口 | 需在当前目标 PVF 中只读确认 | 可确认静态伤害字段存在；不能解释最终伤害公式或武器伤害结算。 |
| 特殊比率 | 需在当前目标 PVF 中只读确认 | 可作为后续定向复核入口；不能证明对人/怪/PVP最终生效。 |
| 元素入口 | 需在当前目标 PVF 中只读确认 | 元素 token 需要按父块读取；拼写变体保留原样。 |
| 异常状态 | 需在当前目标 PVF 中只读确认 | `[active status]` 是状态段入口，不是闭合块；列含义不能静态硬命名。 |
| PVP 覆盖 | 需在当前目标 PVF 中只读确认 | PVP 块是闭合覆盖块，但不是 PVP 公式表。 |
| 未命中字段 | 需在当前目标 PVF 中只读确认 | 跨版本候选；需在当前目标 PVF 中复核 |
| 大写状态上下文 | 需在当前目标 PVF 中只读确认 | 与 `.atk [active status]` 分开记录，不混入本词典。 |

## 可复用规则

- `.atk` 的数字和 token 先按父块解释，再谈运行语义。
- `[active status]` 行列数可变；只读文档中不要把列硬命名为概率、等级、持续、伤害。
- `[pvp] ... [/pvp]` 只覆盖块内字段；读取时必须按块范围切分。
- 根层字段、PVP 块字段、`.act [ACTIVE STATUS]` 和 NUT 运行 API 是不同层，不能互相替代。
- 要讨论命中，必须回到 `.obj/.act/.ani` 和帧级 hitbox；`.atk` 不能独立证明命中。

## 未证明事项

- 不证明实机伤害、攻击倍率、独立攻击力、武器伤害、附加伤害、技攻或最终伤害。
- 不证明元素强化、抗性、属性攻击、多属性或无属性最终生效。
- 不证明异常状态概率、等级、持续、伤害、抗性、免疫、对人/对怪修正或 PVP 修正。
- 不证明 PVP 竞技场最终规则、击退、浮空、硬直、霸体、命中或客户端表现。
- 不证明 owner 链路完整、ANI 攻击框存在、客户端资源完整或服务端放行。
